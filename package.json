{
  "name": "five-day-bible-reading-lookup",
  "version": "0.1.0",
  "description": "Web application to lookup today's reading with the Five-Day Bible Reading plan (fivedaybiblereading.com)",
  "main": "index.js",
  "scripts": {
    "start": "elm-live src/Main.elm -- --output=elm.js --debug",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.2",
    "elm-live": "^4.0.1",
    "serve": "^11.3.0"
  }
}
